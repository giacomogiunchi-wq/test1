{
  "name": "sorting-sim-mvp",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo per simulatore di sorting materiale",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter server start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
